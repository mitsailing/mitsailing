/**
 * Side-effect entry imported by `.storybook/StorybookIntlRoot` only. Raw `.css` in
 * `.storybook` otherwise trips strict TS2882 in Oxlint despite `global.d.ts` `*.css`.
 */

import './global.css';
